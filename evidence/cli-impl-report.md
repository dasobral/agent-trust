exit=101

The CLI implementation is complete in src/main.rs. Build is currently blocked by unrelated in-progress src/mls.rs compilation errors (ProcessedMessage API, moved provider, and roster borrow conflict). No green CLI run is recorded until the library builds.
